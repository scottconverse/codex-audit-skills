# Installation Help

Use this discussion for installation issues.

When posting, include:

- OS and shell
- Codex version if known
- install command used
- output of `python scripts/validate_skills.py`
- whether the skill directories exist under `~/.codex/skills`

Common fix: restart Codex after copying skills so the available-skill list refreshes.
